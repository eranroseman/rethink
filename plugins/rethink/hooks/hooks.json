{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "[ \"$RETHINK_MODE\" = \"on\" ] && { command -v node >/dev/null 2>&1 && node \"${CLAUDE_PLUGIN_ROOT}/hooks/session-start.js\" || cat \"${CLAUDE_PLUGIN_ROOT}/hooks/directive.md\"; } || true",
            "commandWindows": "if ($env:RETHINK_MODE -eq 'on') { if (Get-Command node -ErrorAction SilentlyContinue) { node \"$env:CLAUDE_PLUGIN_ROOT\\hooks\\session-start.js\" } else { Get-Content \"$env:CLAUDE_PLUGIN_ROOT\\hooks\\directive.md\" } }",
            "timeout": 5,
            "statusMessage": "rethink mode (RETHINK_MODE=on)"
          }
        ]
      }
    ]
  }
}
